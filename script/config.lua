config = {
    init_state = 0,

    showHitbox = true,
    showPointerEntities = false,
    logOpenLuaFiles = true,

    states = {
        "menuState",
        "mainState",
        "debugState",
    }
}